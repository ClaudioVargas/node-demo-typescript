# Dependencias y build
node_modules
/dist
/logs
# IDE / editor
.vscode/*

# Entornos locales (¡NUNCA commitear!)
.env
.env.local
.env.staging
.env.production
.env.docker
!.env.example

# Referencia de entornos que contiene secretos reales (no commitear)
ENV-RENDER.md